# NightHub Frontend
A mobile-first, anonymous social platform with a game-like UI.

## Setup
1. Install dependencies: `npm install`
2. Run dev server: `npm run dev`

## Deployment
Hosted on Render with GitHub Actions CI/CD.

